minified file = js file stripped of ALL unnecessary characters spaces, etc.
  MINIMIZES file size

processing or java = "void" instead of "function"
  if keyword is "void" no data is being returned out
  int, float, etc. is the TYPE of data we're sending out
  this is for PROCESSING in JAVA not JAVASCRIPT

renderer (function setup() { createCanvas (x,y, renderer)})
  renderer is 3D enabling aspect of createCanvas
